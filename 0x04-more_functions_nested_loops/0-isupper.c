#include "main.h"

/**
 * _isupper - Check is a letter is uppercase
 * @x: The number to be checked
 * Return: 1 for uppercase letter or 0 if else
 */

int _isupper(int x)

{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
