#include <stdio.h>

/**
 * main - A program that prints number in base 10 starting from 0.
 *
 * Return: always 0
 */

int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
